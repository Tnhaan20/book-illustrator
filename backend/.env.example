PORT=3001
GEMINI_API_KEY=your_gemini_api_key_here
NODE_ENV=development
# Set to "true" to use placeholder images instead of calling the paid image model
# Set to "false" (or remove) to call gemini-3.1-flash-image (requires billing)
GEMINI_IMAGE_MOCK=true
